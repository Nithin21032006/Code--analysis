name: code-analysis-env
version: 1.0
description: AI environment for code analysis

tasks:
  easy:
    grader: true
    reward_range: [0.01, 0.99]

  medium:
    grader: true
    reward_range: [0.01, 0.99]

  hard:
    grader: true
    reward_range: [0.01, 0.99]

api:
  reset: /reset
  step: /step
  state: /state
